from django.conf.urls import include, url
from games.eveonline.modules.audit import views

## BASE
urlpatterns = [
    # url(r'^$', views.dashboard, name='eve-dashboard'),
    # url(r'^create/$', views.create_audit, name='create_audit'),
    # url(r'^delete/$', views.delete_audit, name='delete_audit'),
    # url(r'^view/(?P<audit>\w+)/$', views.view_audit, name='view_audit'),
]
